<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helloworldkotlin">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YoutubeActivity" android:label="Youtube player"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".AdvanceActivity"></activity>
        <activity android:name=".Counter_Activity"></activity>
        <activity android:name=".Calculator_activity"></activity>
        <activity android:name=".Top10Downloader"></activity>
        <activity android:name=".StandAloneActivity" android:label="Standalone Activity"></activity>
        <activity android:name=".youtube_player">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>